{
  "name": "shirokokun-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix portfolio-website",
    "dev:backend": "npm run dev --prefix portfolio-backend",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev\"",
    "build": "cd portfolio-backend && npm install && npm run build",
    "build:backend": "cd portfolio-backend && npm install && npm run build",
    "build:frontend": "cd portfolio-website && npm install && npm run build",
    "start": "cd portfolio-backend && npm start",
    "start:frontend": "npm run start --prefix portfolio-website",
    "install:all": "npm install && npm install --prefix portfolio-backend && npm install --prefix portfolio-website"
  },
  "dependencies": {
    "framer-motion": "^12.23.12",
    "react-intersection-observer": "^9.16.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
